# 批量自动剪辑智能体 - 问题解决方案

## 常见问题及解决方案

### 安装问题

#### 问题1：Python 版本不兼容

**症状**：
```
ModuleNotFoundError: No module named 'pathlib'
```

**解决方案**：
- 确保使用 Python 3.8+
- 检查版本：`python --version`
- 如有问题，重新安装 Python

#### 问题2：FFmpeg 未找到

**症状**：
```
ffmpeg: command not found
```

**解决方案**：

**Windows:**
1. 下载 FFmpeg：https://ffmpeg.org/download.html#build-windows
2. 解压到 `C:\ffmpeg`
3. 添加到系统 PATH：`C:\ffmpeg\bin`
4. 重启命令行窗口

**或使用 Chocolatey:**
```bash
choco install ffmpeg
```

**验证安装:**
```bash
ffmpeg -version
```

#### 问题3：npm 依赖安装失败

**症状**：
```
npm ERR! code ERESOLVE
```

**解决方案**：
```bash
# 清除缓存
npm cache clean --force

# 使用 --legacy-peer-deps
npm install --legacy-peer-deps

# 或使用 cnpm
npm install -g cnpm --registry=https://registry.npmmirror.com
cnpm install
```

### 运行问题

#### 问题4：后端启动失败

**症状**：
```
Address already in use
```

**解决方案**：
```bash
# Windows - 查找占用端口的进程
netstat -ano | findstr :8001
taskkill /PID <进程ID> /F

# 或更换端口
# 修改 backend/start.py 中的 port 为 8002
```

#### 问题5：前端连接后端失败

**症状**：
```
ERR_CONNECTION_REFUSED
```

**解决方案**：
1. 确认后端已启动
2. 检查端口是否正确（默认 8001）
3. 检查防火墙设置
4. 确认 API 地址配置正确

### 配置问题

#### 问题6：飞书连接失败

**症状**：
```
飞书API调用失败: 401 Unauthorized
```

**解决方案**：
1. 检查 App ID 和 App Secret 是否正确
2. 确认应用已发布
3. 检查权限是否正确开通
4. 确认网络连接正常

#### 问题7：OSS 上传失败

**症状**：
```
OSS上传失败: Authentication failed
```

**解决方案**：
1. 检查 Access Key 是否正确
2. 确认 Bucket 名称拼写正确
3. 检查 Endpoint 配置
4. 确认 Bucket 权限设置

### 处理问题

#### 问题8：文案处理失败

**症状**：
```
文案处理失败，未生成场景
```

**解决方案**：
1. 检查文案是否为空
2. 确认文案编码格式（应为 UTF-8）
3. 尝试简化文案内容
4. 检查日志中的详细错误信息

#### 问题9：素材匹配失败

**症状**：
```
所有场景都未找到匹配素材
```

**解决方案**：
1. 检查本地素材路径配置
2. 确认 Pexels API Key 是否有效
3. 尝试使用更通用的关键词
4. 检查网络连接

#### 问题10：配音生成失败

**症状**：
```
edge-tts 不可用
```

**解决方案**：
```bash
# 安装 edge-tts
pip install edge-tts

# 验证安装
edge-tts --version

# 测试生成
edge-tts --text "测试" --write-media test.mp3
```

#### 问题11：视频合成失败

**症状**：
```
视频拼接失败
```

**解决方案**：
1. 确认 FFmpeg 正确安装
2. 检查输出目录权限
3. 确认素材文件格式正确
4. 查看详细错误日志

### 性能问题

#### 问题12：处理速度慢

**症状**：
单个视频处理时间过长

**解决方案**：
1. 减少文案长度
2. 使用本地素材而非网络素材
3. 降低视频分辨率
4. 检查网络带宽

#### 问题13：内存占用过高

**症状**：
```
MemoryError
```

**解决方案**：
1. 减少批量处理数量
2. 清理临时文件
3. 重启服务
4. 增加系统内存

### WebSocket 问题

#### 问题14：WebSocket 连接断开

**症状**：
实时进度更新中断

**解决方案**：
1. 系统会自动重连（最多5次）
2. 检查网络稳定性
3. 确认后端服务正常运行
4. 刷新页面重新连接

### 浏览器兼容性

#### 问题15：某些浏览器无法使用

**症状**：
页面显示异常

**解决方案**：
- 推荐使用 Chrome、Edge 最新版
- Firefox、Safari 也可以使用
- 不支持 IE 浏览器

## 获取帮助

如果以上方案无法解决问题：

1. 查看日志文件
2. 检查 GitHub Issues
3. 提交 Bug 报告

## 调试模式

启用调试日志：

```bash
# 后端
cd backend
set DEBUG=1
python start.py

# 前端
npm run dev -- --debug
```
